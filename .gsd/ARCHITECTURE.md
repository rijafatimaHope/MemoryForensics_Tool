# Architecture

> Auto-generated by /map on 2026-04-05

## Overview

A memory forensics tool being developed for Linux systems in Python. Currently, only integration logic (Role 5) exists. The system acts as a bridge between future Data Extraction (Backend, Roles 1-3) and Visualization (Frontend, Role 4) layers.

```
┌─────────────────────────────────────────┐
│        [GUI / Frontend (Missing)]       │
├─────────────────────────────────────────┤
│       [Integration / Search Layer]      │
│      (integration.py, search_filter.py) │
├─────────────────────────────────────────┤
│      [Data Extraction (Missing)]        │
└─────────────────────────────────────────┘
```

## Components

### Integration Layer (`integration.py`)
- **Purpose:** Prepares raw backend data (processes, connections) for the GUI. Handles cleaning null values, generating summary statistics, and identifying suspicious processes (e.g., malware, rootkit).
- **Location:** `integration.py`
- **Dependencies:** `dummy_data.py`, `search_filter.py`

### Search & Filtering (`search_filter.py`)
- **Purpose:** Provides search functionality for both processes and network connections across multiple attributes (PID, PPID, IP, port, time, name).
- **Location:** `search_filter.py`
- **Dependencies:** None

### Test & Mock Data (`dummy_data.py`, `test_backend_data.py`)
- **Purpose:** Simulates backend JSON data dumps for frontend integration testing.
- **Location:** `dummy_data.py`, `test_backend_data.py`
- **Dependencies:** `integration.py`, `json`

## Data Flow

1. Backend memory extraction tools generate JSON/raw dictionaries of `processes` and `connections`. (Mocked currently).
2. Data is passed to `prepare_data_for_gui()` in `integration.py`.
3. Nulls are scrubbed. Stats and suspicious counts are attached. Format is normalized.
4. User initiates search via GUI, handled by `search_in_prepared_data()` which calls functions in `search_filter.py`.

## Technical Debt

- [ ] Backend data extraction logic does not exist. (Roles 1-3).
- [ ] Frontend GUI completely missing. (Role 4).
- [ ] `count_suspicious()` uses very rudimentary string matching and is not robust.
- [ ] Test code is mingled in operational files such as `integration.py` and `search_filter.py`.

## Conventions

**Naming:** Snake case functions and variables (`prepare_data_for_gui`, `sample_processes`).
**Testing:** Basic print-based manual testing scripts. No unit testing framework like `pytest`.
